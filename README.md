# rakudo

Rakudo built from (nxadm)[https://nxadm.github.io/rakudo-pkg/] for debian:10.

## Use

```
docker run -it --rm $(id -u)
    --workdir /work
    --volume  $WORKDIR:/work
    benlittle6/rakudo
```

